spring.application.name=crud-relacional
# Nombre y Puerto
server.port=8080

# Conexión a MySQL (Cambia 'root' y tu clave local de MySQL en tu compu)
spring.datasource.url=jdbc:mysql://localhost:3306/bd_inventario?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=leon22

# Configuración mágica de JPA/Hibernate (Crea las tablas automáticamente con las relaciones)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect